{
  "name": "local-file-converter",
  "version": "1.0.0",
  "author": "Recep Özen",
  "repository": {
    "type": "git",
    "url": "https://github.com/Sergeant61/local-privacy-converter.git"
  },
  "homepage": "https://github.com/Sergeant61/local-privacy-converter",
  "bugs": "https://github.com/Sergeant61/local-privacy-converter/issues",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{js,ts,svelte,json,md}\"",
    "dist": "turbo run build && node scripts/prepare-ffmpeg.mjs && pnpm --filter desktop exec electron-builder --publish never",
    "dist:mac": "turbo run build && node scripts/prepare-ffmpeg.mjs && pnpm --filter desktop exec electron-builder --mac --publish never",
    "dist:win": "turbo run build && node scripts/prepare-ffmpeg.mjs && pnpm --filter desktop exec electron-builder --win --publish never",
    "dist:linux": "turbo run build && node scripts/prepare-ffmpeg.mjs && pnpm --filter desktop exec electron-builder --linux --publish never",
    "dist:dir": "turbo run build && node scripts/prepare-ffmpeg.mjs && pnpm --filter desktop exec electron-builder --dir --publish never"
  },
  "devDependencies": {
    "@lfc/config": "workspace:*",
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-plugin-svelte": "^3.14.1",
    "globals": "^16.5.0",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.3",
    "svelte": "^5.46.1",
    "turbo": "^2.7.6",
    "typescript": "~5.7.3",
    "typescript-eslint": "^8.52.1"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}